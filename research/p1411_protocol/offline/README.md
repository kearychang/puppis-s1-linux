# Offline protocol inspector

Run from the repository root:

```bash
python3 -m research.p1411_protocol.offline.tui
```

The inspector decodes built-in redacted fixtures, validates pasted frames, and locally encodes getter requests. It performs no network I/O.
